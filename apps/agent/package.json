{
  "name": "agent",
  "version": "1.0.0",
  "description": "Agent API",
  "scripts": {
    "dev": "uv run fastapi dev chat/src/chat/api.py --host 0.0.0.0 --port 8001",
    "test": "cd chat && uv run pytest tests/ -v"
  }
}
