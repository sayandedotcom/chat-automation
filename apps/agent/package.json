{
  "name": "agent",
  "version": "1.0.0",
  "description": "Agent API",
  "scripts": {
    "dev": "uv run --python 3.14 --package chat fastapi dev chat/src/chat/api.py --host 0.0.0.0 --port 8001",
    "studio": "cd chat && uv run langgraph dev",
    "test": "cd chat && uv run pytest tests/ -v"
  }
}
