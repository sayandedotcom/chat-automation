{
  "name": "agent",
  "version": "1.0.0",
  "description": "Agent API",
  "scripts": {
    "dev": "uv run --package chat fastapi dev chat/src/chat/api.py --host 0.0.0.0 --port 8001",
    "studio": "cd chat && uv run langgraph dev",
    "test": "cd chat && uv run pytest tests/ -v"
  }
}
